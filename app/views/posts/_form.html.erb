<%= form_for @post do |f| %>
  <%= render 'shared/errors', object: f.object %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit t 'views.submit' %>
<% end %>
