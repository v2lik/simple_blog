<li>
  <%= link_to post do %>
    <%= image_tag post.user.avatar %>
    <%= post.title %>
    <%= post.body %>
  <% end %>
  <% if owner?(post) %>
    <%= link_to t('views.edit'), edit_post_path(post) %>
    <%= link_to t('views.destroy'), post, method: :delete, data: { confirm: t('views.confirm') }, remote:  true, class: 'delete-post' %>
  <% end %>
</li>
