<%= form_for :session, url: sessions_path do |f| %>
  <%= f.label :login %>
  <%= f.text_field :login %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit t 'views.submit' %>
<% end %>

<%= link_to t('views.sign_up'), new_user_path %>